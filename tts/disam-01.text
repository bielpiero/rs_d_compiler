function entrance(x, y)
	var u = #1
	var hola = #5
	var pepito = #-3
	goto(#2, #5, #0.9)
	if(hola == u and pepito == "mañana" or (origin() < (#3 + #4)))
		say("no")
	endif
	return #3
endfunction

function origin()
	var u = "ooooo"
	return u
endfunction

function main()
	entrance()
endfunction